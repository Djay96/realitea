[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Hourly aggregation + summarization job.
[functions."refresh-news"]
  # Allow enough time for fetching sources + DeepSeek summarization.
  timeout = 120

[functions]
  node_bundler = "esbuild"
